# 更新记录

按语义化版本记录。每个里程碑（M1-M20）对应一段有依赖关系的
连续开发：模型 → 链接图 → 索引 → 标签 → 查询 → 导出 →
模板 → 校验 → 备份 → 编辑 → 时间线 → 报表 → 相似 → 配置 →
漫游 → 摘要 → 导入 → 基准 → 建议 → CLI 汇总。

## [0.1.0] - 2026-09-24

首个可用版本（M1-M20 全量）。

### 核心

- **笔记模型**（M1）：frontmatter 解析（坏 YAML 不炸、正文保留）、
  原子写回（临时文件 + rename）、分层笔记名（`sub/page`）。
- **链接图**（M2）：wikilink 提取（代码块/行内代码不算）、
  双向链接、悬空链接、孤岛检测。
- **倒排索引**（M3）：中英混合（英文词元 + 中文 bigram）、
  位置级短语检索（偏移连续性判定保序）、前缀检索。
- **标签体系**（M4）：列表/逗号串/单值归一、include AND /
  exclude NOT 过滤。
- **查询语言**（M5）：词/短语/tag/-tag/link/orphan/unresolved
  组合查询，AND 语义。
- **静态导出**（M6）：完全离线 HTML（零外部资源）、反向链接区、
  悬空链接红字、XSS 全链路转义。
- **模板变量**（M7）：`{{key}}`/`{{name}}`/`{{today}}`/`{{now}}`、
  模板注释、未定义键保留原文。
- **健康校验**（M8）：坏 frontmatter/名字冲突（error）、
  悬空/孤岛/空笔记/坏模板（warning），CLI 退出码分级。
- **备份**（M9）：时间戳 zip + 秒级撞名防护 + 保留策略 +
  恢复（旧目录改名保留退路、zip slip 防护）。
- **编辑**（M10）：rename 全库引用同步（含别名形态）、
  delete 自动备份、标签增删幂等。
- **时间线**（M11）：每日笔记约定（YYYY-MM-DD）、修改时间
  倒序时间线、连续天数 streak（今日断档不中断）。
- **统计报表**（M12）：JSON + Markdown 双格式（标签热度、
  知识枢纽、索引规模）。
- **相似笔记**（M13）：Jaccard 词元重合、稀疏共现对计算。
- **配置**（M14）：`notesdb.json` + `NDB_*` 环境变量，
  未知键/类型错/坏 JSON 全部报错。
- **随机漫游**（M15）：带去重的链接图随机游走（seed 可复现）。
- **搜索摘要**（M16）：命中行上下文合并、词元边界高亮
  （大小写安全、HTML 转义先于注入）。
- **批量导入**（M17）：skip/suffix/overwrite 三策略、
  子目录结构保留、坏文件不中断整批。
- **性能基准**（M18）：合成数据回归探测器 + CI 预算断言。
- **链接建议**（M19）：相似未链接推荐、悬空链接模糊修复
  （Levenshtein）。
- **CLI**（M20）：20 个子命令全量接线，端到端测试走真子进程。
